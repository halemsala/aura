# AURA QUANT-X 12.7.0 — dependências do serviço de voz
# O Ollama é um processo separado e não é instalado por pip.
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.8,<3
faster-whisper==1.2.1
pyyaml==6.0.1
requests>=2.32,<3
numpy>=1.26,<3
websockets>=12,<16
sounddevice>=0.5,<1
ctranslate2>=4.5,<5
edge-tts>=6.1.0
pyzmq>=27.2,<28

# TTS premium opcional; não é necessário para o boot do servidor.
# coqui-tts==0.27.5
# transformers>=4.57.1,<5.0
# huggingface-hub>=0.26.0,<1.0
# NAO instalar torchcodec no Windows sem FFmpeg full-shared.

# TTS offline opcional.
# Instale o binário piper e um modelo pt-BR (.onnx).
# Variáveis: AURA_PIPER_BIN, AURA_PIPER_MODEL, AURA_LOCAL_ONLY=1
